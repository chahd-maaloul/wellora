{% extends 'layouts/auth.html.twig' %}
{% set auth_container_class = 'max-w-5xl' %}

{% block title %}Mot de passe oublié - WellCare Connect{% endblock %}

{% block auth_heading %}{% endblock %}
{% block auth_description %}{% endblock %}

{% block auth_form %}
  <div class="grid grid-cols-1 gap-0 lg:grid-cols-12 min-h-[550px]">
    {# Left Panel - Visual & Trust Elements #}
    <div class="hidden lg:col-span-5 lg:flex lg:flex-col">
      <div class="relative flex flex-col h-full p-8 overflow-hidden bg-gradient-to-br from-wellcare-500 via-wellcare-600 to-wellcare-700 rounded-l-2xl">
        {# Animated Decorative Elements #}
        <div class="absolute top-0 right-0 w-64 h-64 -mt-20 -mr-20 rounded-full bg-white/10 blur-3xl animate-pulse"></div>
        <div class="absolute bottom-0 left-0 w-48 h-48 -mb-16 -ml-16 rounded-full bg-wellcare-400/20 blur-2xl"></div>
        
        <div class="relative z-10 flex flex-col h-full">
          {# Back Link with enhanced styling #}
          <a href="{{ path('app_login') }}" class="inline-flex items-center gap-2 mb-8 transition-colors text-white/80 hover:text-white group">
            <div class="flex items-center justify-center w-8 h-8 transition-colors rounded-lg bg-white/10 group-hover:bg-white/20">
              <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
              </svg>
            </div>
            <span class="text-sm font-medium">Retour à la connexion</span>
          </a>

          {# Main Content #}
          <div class="flex flex-col justify-center flex-1">
            <div class="flex items-center justify-center w-20 h-20 mb-6 shadow-lg bg-white/20 rounded-2xl backdrop-blur-sm">
              <svg class="w-10 h-10 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
            </div>
            
            <h2 class="mb-4 text-2xl font-bold text-white">Récupération du mot de passe</h2>
            <p class="text-sm leading-relaxed text-wellcare-100">
              Nous vous enverrons un lien sécurisé pour réinitialiser votre mot de passe. Pour votre sécurité, ce lien expirera dans 1 heure.
            </p>

            {# Security Info with enhanced styling #}
            <div class="mt-8 space-y-4">
              <div class="flex items-center gap-3 p-4 transition-all duration-300 border bg-white/10 rounded-xl backdrop-blur-sm border-white/10 hover:bg-white/15">
                <div class="flex items-center justify-center rounded-lg shadow-inner w-11 h-11 bg-white/20">
                  <svg class="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                  </svg>
                </div>
                <div>
                  <p class="text-sm font-semibold text-white">Lien sécurisé</p>
                  <p class="text-xs text-wellcare-100">Expiration dans 1 heure</p>
                </div>
              </div>

              <div class="flex items-center gap-3 p-4 transition-all duration-300 border bg-white/10 rounded-xl backdrop-blur-sm border-white/10 hover:bg-white/15">
                <div class="flex items-center justify-center rounded-lg shadow-inner w-11 h-11 bg-white/20">
                  <svg class="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/>
                  </svg>
                </div>
                <div>
                  <p class="text-sm font-semibold text-white">Vérification par email</p>
                  <p class="text-xs text-wellcare-100">Envoyé à votre adresse enregistrée</p>
                </div>
              </div>

              <div class="flex items-center gap-3 p-4 transition-all duration-300 border bg-white/10 rounded-xl backdrop-blur-sm border-white/10 hover:bg-white/15">
                <div class="flex items-center justify-center rounded-lg shadow-inner w-11 h-11 bg-white/20">
                  <svg class="w-5 h-5 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                </div>
                <div>
                  <p class="text-sm font-semibold text-white">Usage unique</p>
                  <p class="text-xs text-wellcare-100">Le lien ne peut être utilisé qu'une seule fois</p>
                </div>
              </div>
            </div>
          </div>

          {# Help Text #}
          <div class="pt-8 mt-auto border-t border-white/20">
            <p class="text-xs text-center text-wellcare-100">
              Besoin d'aide ? Contact :<br>
              <a href="mailto:support@wellora.com" class="font-medium text-white hover:underline">support@wellora.com</a>
            </p>
          </div>
        </div>
      </div>
    </div>

    {# Right Panel - Form #}
    <div class="lg:col-span-7">
      <div class="flex flex-col justify-center h-full p-8 lg:p-10">
        {# Mobile Header #}
        <div class="mb-8 text-center lg:hidden">
          <div class="flex items-center justify-center w-16 h-16 mx-auto mb-4 bg-wellcare-100 rounded-2xl">
            <svg class="w-8 h-8 text-wellcare-600" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
            </svg>
          </div>
          <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Mot de passe oublié ?</h1>
          <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">Pas de souci, nous vous enverrons les instructions de réinitialisation.</p>
        </div>

        {# Desktop Header #}
        <div class="hidden mb-8 lg:block">
          <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Mot de passe oublié ?</h1>
          <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">
            Entrez votre adresse email et nous vous enverrons un lien pour réinitialiser votre mot de passe.
          </p>
        </div>

        {# Flash Messages #}
        {% for flash_error in app.flashes('error') %}
          <div class="p-4 mb-6 text-sm text-red-700 bg-red-100 rounded-lg dark:bg-red-900 dark:text-red-300" role="alert">
            <span class="font-medium">Erreur!</span> {{ flash_error }}
          </div>
        {% endfor %}

        {% for flash_success in app.flashes('success') %}
          <div class="p-4 mb-6 text-sm text-green-700 bg-green-100 rounded-lg dark:bg-green-900 dark:text-green-300" role="alert">
            <span class="font-medium">Succès!</span> {{ flash_success }}
          </div>
        {% endfor %}

        {# Form #}
        <form 
          class="space-y-6" 
          action="{{ path('app_forgot_password') }}" 
          method="POST"
        >
          <input type="hidden" name="_csrf_token" value="{{ csrf_token('forgot_password') }}">

          <div>
            <label for="email" class="block mb-2 text-sm font-medium text-gray-900 dark:text-white">
              Adresse email
            </label>
            <div class="relative">
              <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                <svg class="w-4 h-4 text-gray-500 dark:text-gray-400" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="currentColor" viewBox="0 0 20 20">
                  <path d="M10 0a10 10 0 1 0 10 10A10.011 10.011 0 0 0 10 0Zm0 5a3 3 0 1 1 0 6 3 3 0 0 1 0-6Zm0 13a8.949 8.949 0 0 1-4.951-1.488A3.987 3.987 0 0 1 9 13h2a3.987 3.987 0 0 1 3.951 3.512A8.949 8.949 0 0 1 10 18Z"/>
                </svg>
              </div>
              <input 
                type="email" 
                name="email" 
                id="email"
                value="{{ email|default('') }}"
                class="bg-gray-50 border border-gray-300 text-gray-900 text-sm rounded-lg focus:ring-wellcare-500 focus:border-wellcare-500 block w-full pl-10 p-2.5 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-wellcare-500 dark:focus:border-wellcare-500" 
                placeholder="vous@exemple.com" 
                required
              >
            </div>
          </div>

          <button 
            type="submit" 
            class="w-full text-white bg-wellcare-600 hover:bg-wellcare-700 focus:ring-4 focus:outline-none focus:ring-wellcare-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-wellcare-600 dark:hover:bg-wellcare-700 dark:focus:ring-wellcare-800"
          >
            Envoyer le lien de réinitialisation
          </button>

          <div class="text-sm font-medium text-gray-500 dark:text-gray-400">
            Pas encore de compte ? 
            <a href="{{ path('app_register_patient') }}" class="text-wellcare-600 hover:underline dark:text-wellcare-500">Créer un compte</a>
          </div>
        </form>

        {# Back to login link #}
        <div class="mt-6 text-center lg:hidden">
          <a href="{{ path('app_login') }}" class="text-sm text-wellcare-600 hover:underline dark:text-wellcare-500">
            ← Retour à la connexion
          </a>
        </div>
      </div>
    </div>
  </div>
{% endblock %}
