{% extends 'base.html.twig' %}

{% block title %}Email vérifié - WellCare Connect{% endblock %}

{% block body_class %}bg-gray-50 dark:bg-gray-900{% endblock %}

{% block body %}
<div class="flex flex-col min-h-screen">
  {# Header #}
  <header class="bg-white border-b border-gray-200 dark:bg-gray-800 dark:border-gray-700">
    <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center gap-3">
          <a href="/" class="flex items-center gap-3">
            <div class="flex items-center justify-center w-10 h-10 bg-wellcare-100 dark:bg-wellcare-900/30 rounded-xl">
              <img src="{{ asset('build/images/logo-icon.svg') }}" alt="Wellora" class="w-6 h-6">
            </div>
            <span class="text-xl font-bold text-gray-900 dark:text-white">Wellora</span>
          </a>
        </div>
        <nav class="flex items-center gap-4">
          <a href="/" class="text-sm font-medium text-gray-700 dark:text-gray-300 hover:text-wellcare-600 dark:hover:text-wellcare-400">
            Accueil
          </a>
          <a href="{{ path('app_login') }}" class="text-sm font-medium text-wellcare-600 dark:text-wellcare-400 hover:text-wellcare-700 dark:hover:text-wellcare-300">
            Connexion
          </a>
        </nav>
      </div>
    </div>
  </header>

  {# Main Content #}
  <main class="flex items-center justify-center flex-1 px-4 py-12 sm:px-6 lg:px-8">
    <div class="w-full max-w-md">
      {# Success Card #}
      <div class="p-8 bg-white shadow-xl dark:bg-gray-800 rounded-2xl">
        {# Theme Toggle #}
        <div class="flex justify-end mb-6">
          <button id="theme-toggle" type="button" class="text-gray-500 dark:text-gray-400 hover:bg-gray-100 dark:hover:bg-gray-700 focus:outline-none focus:ring-4 focus:ring-gray-200 dark:focus:ring-gray-700 rounded-lg text-sm p-2.5">
            <svg id="theme-toggle-dark-icon" class="hidden w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"></path>
            </svg>
            <svg id="theme-toggle-light-icon" class="hidden w-5 h-5" fill="currentColor" viewBox="0 0 20 20">
              <path d="M10 2a1 1 0 011 1v1a1 1 0 11-2 0V3a1 1 0 011-1zm4 8a4 4 0 11-8 0 4 4 0 018 0zm-.464 4.95l.707.707a1 1 0 001.414-1.414l-.707-.707a1 1 0 00-1.414 1.414zm2.12-10.607a1 1 0 010 1.414l-.706.707a1 1 0 11-1.414-1.414l.707-.707a1 1 0 011.414 0zM17 11a1 1 0 100-2h-1a1 1 0 100 2h1zm-7 4a1 1 0 011 1v1a1 1 0 11-2 0v-1a1 1 0 011-1zM5.05 6.464A1 1 0 106.465 5.05l-.708-.707a1 1 0 00-1.414 1.414l.707.707zm1.414 8.486l-.707.707a1 1 0 01-1.414-1.414l.707-.707a1 1 0 011.414 1.414zM4 11a1 1 0 100-2H3a1 1 0 000 2h1z" fill-rule="evenodd" clip-rule="evenodd"></path>
            </svg>
          </button>
        </div>

        {# Success Icon #}
        <div class="flex justify-center mb-6">
          <div class="flex items-center justify-center w-20 h-20 bg-green-100 rounded-full dark:bg-green-900/30">
            <svg class="w-10 h-10 text-green-600 dark:text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
          </div>
        </div>

        {# Title #}
        <div class="mb-6 text-center">
          <h1 class="mb-2 text-2xl font-bold text-gray-900 dark:text-white">Félicitations !</h1>
          <p class="text-gray-600 dark:text-gray-400">
            {% if userType == 'professional' %}
              Votre adresse email a été vérifiée avec succès !<br>
              <span class="text-wellcare-600 dark:text-wellcare-400">Un administrateur va vérifier vos documents sous 24-48 heures.</span>
            {% else %}
              Votre adresse email a été vérifiée avec succès.
            {% endif %}
          </p>
        </div>

        {% if userType == 'professional' %}
          {# Professional-specific message #}
          <div class="p-4 mb-8 border bg-wellcare-50 dark:bg-wellcare-900/20 border-wellcare-200 dark:border-wellcare-800 rounded-xl">
            <div class="flex items-start gap-3">
              <svg class="w-6 h-6 text-wellcare-600 dark:text-wellcare-400 mt-0.5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <div>
                <h3 class="text-sm font-semibold text-wellcare-800 dark:text-wellcare-300">
                  Étape suivante pour les professionnels de santé
                </h3>
                <p class="mt-1 text-sm text-wellcare-700 dark:text-wellcare-400">
                  Un administrateur va vérifier vos documents sous <strong>24-48 heures</strong>.
                  Vous recevrez un email de confirmation une fois votre compte validé.
                </p>
              </div>
            </div>
          </div>
        {% endif %}

        {# Checkmarks #}
        <div class="mb-8 space-y-3">
          <div class="flex items-center gap-3 text-green-600 dark:text-green-400">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span class="text-sm font-medium">Votre adresse email est vérifiée</span>
          </div>
          {% if userType == 'professional' %}
          <div class="flex items-center gap-3 text-wellcare-600 dark:text-wellcare-400">
            <svg class="w-5 h-5 animate-pulse" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
            </svg>
            <span class="text-sm font-medium">En attente de validation admin (24-48h)</span>
          </div>
          {% else %}
          <div class="flex items-center gap-3 text-green-600 dark:text-green-400">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span class="text-sm font-medium">Votre compte est maintenant activé</span>
          </div>
          <div class="flex items-center gap-3 text-green-600 dark:text-green-400">
            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
            </svg>
            <span class="text-sm font-medium">Vous pouvez accéder à tous nos services</span>
          </div>
          {% endif %}
        </div>

        {# Login Button #}
        <a href="{{ path('app_login') }}" class="flex items-center justify-center gap-2 w-full bg-wellcare-600 hover:bg-wellcare-700 text-white font-semibold py-3 px-4 rounded-xl shadow-lg shadow-wellcare-500/30 hover:shadow-wellcare-500/40 transition-all duration-200 hover:-translate-y-0.5">
          <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/>
          </svg>
          Se connecter
        </a>

        {# Features Preview #}
        <div class="pt-6 mt-8 border-t border-gray-200 dark:border-gray-700">
          {% if userType == 'professional' %}
            <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">Après validation de votre compte par l'administrateur, vous pourrez :</p>
          {% else %}
            <p class="mb-4 text-sm text-gray-600 dark:text-gray-400">Après connexion, vous pourrez :</p>
          {% endif %}
          <div class="grid grid-cols-4 gap-4 text-center">
            <div class="flex flex-col items-center gap-2">
              <div class="flex items-center justify-center w-12 h-12 bg-wellcare-50 dark:bg-wellcare-900/20 rounded-xl">
                <svg class="w-6 h-6 text-wellcare-600 dark:text-wellcare-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
              </div>
              <span class="text-xs text-gray-600 dark:text-gray-400">🏥</span>
            </div>
            <div class="flex flex-col items-center gap-2">
              <div class="flex items-center justify-center w-12 h-12 bg-wellcare-50 dark:bg-wellcare-900/20 rounded-xl">
                <svg class="w-6 h-6 text-wellcare-600 dark:text-wellcare-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"/>
                </svg>
              </div>
              <span class="text-xs text-gray-600 dark:text-gray-400">📋</span>
            </div>
            <div class="flex flex-col items-center gap-2">
              <div class="flex items-center justify-center w-12 h-12 bg-wellcare-50 dark:bg-wellcare-900/20 rounded-xl">
                <svg class="w-6 h-6 text-wellcare-600 dark:text-wellcare-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <span class="text-xs text-gray-600 dark:text-gray-400">🍎</span>
            </div>
            <div class="flex flex-col items-center gap-2">
              <div class="flex items-center justify-center w-12 h-12 bg-wellcare-50 dark:bg-wellcare-900/20 rounded-xl">
                <svg class="w-6 h-6 text-wellcare-600 dark:text-wellcare-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 10l4.553-2.276A1 1 0 0121 8.618v6.764a1 1 0 01-1.447.894L15 14M5 18h8a2 2 0 002-2V8a2 2 0 00-2-2H5a2 2 0 00-2 2v8a2 2 0 002 2z"/>
                </svg>
              </div>
              <span class="text-xs text-gray-600 dark:text-gray-400">📹</span>
            </div>
          </div>
        </div>
      </div>

      {# Footer Links #}
      <div class="mt-6 text-center">
        <div class="flex items-center justify-center gap-4 text-sm text-gray-500 dark:text-gray-400">
          <a href="/terms" class="hover:text-wellcare-600 dark:hover:text-wellcare-400">Conditions</a>
          <span>|</span>
          <a href="/privacy" class="hover:text-wellcare-600 dark:hover:text-wellcare-400">Confidentialité</a>
        </div>
        <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">© 2026 WellCare Connect.</p>
      </div>
    </div>
  </main>
</div>

{% endblock %}

{% block javascripts %}
{{ parent() }}
<script>
document.addEventListener('DOMContentLoaded', function() {
  // Theme toggle functionality
  var themeToggleDarkIcon = document.getElementById('theme-toggle-dark-icon');
  var themeToggleLightIcon = document.getElementById('theme-toggle-light-icon');
  var themeToggleBtn = document.getElementById('theme-toggle');

  // Change the icons inside the button based on previous settings
  if (localStorage.getItem('color-theme') === 'dark' || (!('color-theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
    themeToggleLightIcon.classList.remove('hidden');
  } else {
    themeToggleDarkIcon.classList.remove('hidden');
  }

  themeToggleBtn.addEventListener('click', function() {
    // toggle icons inside button
    themeToggleDarkIcon.classList.toggle('hidden');
    themeToggleLightIcon.classList.toggle('hidden');

    // if set via local storage previously
    if (localStorage.getItem('color-theme')) {
      if (localStorage.getItem('color-theme') === 'light') {
        document.documentElement.classList.add('dark');
        localStorage.setItem('color-theme', 'dark');
      } else {
        document.documentElement.classList.remove('dark');
        localStorage.setItem('color-theme', 'light');
      }
    } else {
      if (document.documentElement.classList.contains('dark')) {
        document.documentElement.classList.remove('dark');
        localStorage.setItem('color-theme', 'light');
      } else {
        document.documentElement.classList.add('dark');
        localStorage.setItem('color-theme', 'dark');
      }
    }
  });
});
</script>
{% endblock %}
