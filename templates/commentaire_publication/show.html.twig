{% extends 'base.html.twig' %}

{% block title %}CommentairePublication{% endblock %}

{% block body %}
    <h1>CommentairePublication</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_publication.id }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ commentaire_publication.commentaire }}</td>
            </tr>
            <tr>
                <th>DateCommentaire</th>
                <td>{{ commentaire_publication.dateCommentaire ? commentaire_publication.dateCommentaire|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_publication_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_publication_edit', {'id': commentaire_publication.id}) }}">edit</a>

    {{ include('commentaire_publication/_delete_form.html.twig') }}
{% endblock %}
