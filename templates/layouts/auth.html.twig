<!DOCTYPE html>
<html lang="fr" class="h-full scroll-smooth">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="WellCare Connect - Plateforme de santé et téléconsultation pour patients et professionnels de santé">
    <meta name="theme-color" content="#00A790">
    <title>{% block title %}WellCare Connect{% endblock %}</title>
    
    {# Preconnect for performance #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+ans:wght@400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
    
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
    {% block scripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</head>
<body class="h-full antialiased text-gray-900 bg-white dark:bg-gray-950 dark:text-gray-100">
    {# Skip to main content link for keyboard users #}
    <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 focus:z-50 focus:px-4 focus:py-2 focus:bg-wellcare-600 focus:text-white focus:rounded-lg">
        {{ 'Aller au contenu principal'|trans }}
    </a>
    
    <div id="app" class="flex flex-col min-h-full">
        
        {# Flash Messages - Auto-dismiss after 7 seconds with dark/light mode support #}
        <div x-data="{
            init() {
                this.$nextTick(() => {
                    const alerts = this.$el.querySelectorAll('.flash-alert');
                    alerts.forEach((alert, index) => {
                        setTimeout(() => {
                            alert.style.opacity = '0';
                            alert.style.transform = 'translateX(100%)';
                            setTimeout(() => alert.remove(), 500);
                        }, 7000 + (index * 500));
                    });
                });
            }
        }">
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash-alert p-4 mb-4 mx-4 mt-4 text-sm rounded-lg transition-all duration-500 ease-in-out {% if type == 'error' %}bg-red-100 text-red-700 dark:bg-red-900/50 dark:text-red-300 border border-red-200 dark:border-red-700{% elseif type == 'warning' %}bg-yellow-100 text-yellow-700 dark:bg-yellow-900/50 dark:text-yellow-300 border border-yellow-200 dark:border-yellow-700{% elseif type == 'success' %}bg-green-100 text-green-700 dark:bg-green-900/50 dark:text-green-300 border border-green-200 dark:border-green-700{% else %}bg-blue-100 text-blue-700 dark:bg-blue-900/50 dark:text-blue-300 border border-blue-200 dark:border-blue-700{% endif %}" role="alert">
                    <div class="flex items-start gap-3">
                        <div class="flex-shrink-0 mt-0.5">
                            {% if type == 'error' %}
                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                </svg>
                            {% elseif type == 'warning' %}
                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                                </svg>
                            {% elseif type == 'success' %}
                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                </svg>
                            {% else %}
                                <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                </svg>
                            {% endif %}
                        </div>
                        <div class="flex-1">
                            <span class="font-semibold">
                                {% if type == 'error' %}
                                    Erreur:
                                {% elseif type == 'warning' %}
                                    Attention:
                                {% elseif type == 'success' %}
                                    Succès:
                                {% else %}
                                    Info:
                                {% endif %}
                            </span>
                            <p class="mt-1">{{ message }}</p>
                        </div>
                        <button @click="$event.target.closest('.flash-alert').style.opacity='0'; $event.target.closest('.flash-alert').style.transform='translateX(100%)'; setTimeout(() => $event.target.closest('.flash-alert').remove(), 500)" class="flex-shrink-0 ml-2 text-current transition-opacity opacity-60 hover:opacity-100">
                            <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                            </svg>
                        </button>
                    </div>
                </div>
            {% endfor %}
        {% endfor %}
        </div>

        {# Clean Header - Logo + Full Navigation #}
        <header class="sticky top-0 z-40 border-b border-gray-200 bg-white/80 dark:bg-gray-950/80 backdrop-blur-md dark:border-gray-800" x-data="{ mobileOpen: false }">
            <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
                <div class="flex justify-between h-16">
                    <div class="flex items-center space-x-4">
                        <a href="{{ path('app_home') }}" class="flex items-center gap-2 text-xl font-bold transition-opacity text-wellcare-600 dark:text-wellcare-400 hover:opacity-80">
                            <img src="{{ asset('build/images/logo-icon.svg') }}" alt="Wellora" class="h-9 w-9">
                            <span>Wellora</span>
                        </a>
                        <nav class="hidden ml-8 space-x-1 text-sm md:flex" x-data="{ activeLink: window.location.pathname }">
                            <a href="{{ path('app_home') }}" 
                               class="nav-item-magnetic"
                               :class="activeLink === '/' ? 'nav-link-active' : 'nav-link'"
                               @click="activeLink = '/'">
                                <span class="flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                                    </svg>
                                    Accueil
                                </span>
                            </a>
                            <a href="{{ path('app_login') }}" 
                               class="nav-item-magnetic"
                               :class="activeLink === '/login' ? 'nav-link-active' : 'nav-link'"
                               @click="activeLink = '/login'">
                                <span class="flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/>
                                    </svg>
                                    Connexion
                                </span>
                            </a>
                            <a href="{{ path('app_register_patient') }}" 
                               class="nav-item-magnetic"
                               :class="activeLink === '/register/patient' ? 'nav-link-active' : 'nav-link'"
                               @click="activeLink = '/register/patient'">
                                <span class="flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"/>
                                    </svg>
                                    Inscription
                                </span>
                            </a>
                            <button type="button" class="nav-item-magnetic nav-link hover:text-wellcare-600 hover:bg-wellcare-50 dark:hover:bg-wellcare-900/20" onclick="document.getElementById('professionalModal').classList.remove('hidden')">
                                <span class="flex items-center gap-2">
                                    <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/>
                                    </svg>
                                    Professionnel
                                </span>
                            </button>
                        </nav>
                    </div>
                    <div class="flex items-center gap-2">
                        <button
                            type="button"
                            class="inline-flex items-center justify-center p-2 text-gray-700 transition-colors bg-white border border-gray-200 rounded-lg shadow-sm hover:bg-gray-50 md:hidden dark:border-gray-800 dark:bg-gray-950 dark:text-gray-200 dark:hover:bg-gray-900"
                            @click="mobileOpen = !mobileOpen"
                            aria-label="Toggle menu"
                        >
                            <svg x-show="!mobileOpen" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                            </svg>
                            <svg x-show="mobileOpen" class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor" style="display: none;">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                            </svg>
                        </button>
                        <button
                            type="button"
                            class="inline-flex items-center gap-2 px-3 py-2 text-sm text-gray-700 transition-colors bg-white border border-gray-200 rounded-lg shadow-sm hover:bg-gray-50 dark:border-gray-800 dark:bg-gray-950 dark:text-gray-200 dark:hover:bg-gray-900"
                            x-data
                            @click="$store.theme.toggle()"
                            aria-label="Toggle theme"
                        >
                            <span x-show="!$store.theme.isDark">
                                <svg class="w-4 h-4 text-yellow-500" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 3v1m0 16v1m9-9h-1M4 12H3m15.364 6.364l-.707-.707M6.343 6.343l-.707-.707m12.728 0l-.707.707M6.343 17.657l-.707.707M16 12a4 4 0 11-8 0 4 4 0 018 0z"/>
                                </svg>
                            </span>
                            <span x-show="$store.theme.isDark" style="display: none;">
                                <svg class="w-4 h-4 text-wellcare-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M20.354 15.354A9 9 0 018.646 3.646 9.003 9.003 0 0012 21a9.003 9.003 0 008.354-5.646z"/>
                                </svg>
                            </span>
                            <span class="hidden text-xs sm:inline" x-text="$store.theme.isDark ? 'Clair' : 'Sombre'"></span>
                        </button>
                    </div>
                </div>
            </div>

            {# Mobile menu #}
            <div class="border-t border-gray-200 md:hidden dark:border-gray-800" x-show="mobileOpen" x-transition:enter="transition ease-out duration-200" x-transition:enter-start="opacity-0 -translate-y-2" x-transition:enter-end="opacity-100 translate-y-0" x-transition:leave="transition ease-in duration-150" x-transition:leave-start="opacity-100 translate-y-0" x-transition:leave-end="opacity-0 -translate-y-2" style="display: none;">
                <div class="px-4 py-3 space-y-1">
                    <a href="{{ path('app_home') }}" class="mobile-nav-link group">
                        <span class="mobile-nav-icon">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"/>
                            </svg>
                        </span>
                        <span class="mobile-nav-text">Accueil</span>
                    </a>
                    <a href="{{ path('app_login') }}" class="mobile-nav-link group">
                        <span class="mobile-nav-icon">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11 16l-4-4m0 0l4-4m-4 4h14m-5 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h7a3 3 0 013 3v1"/>
                            </svg>
                        </span>
                        <span class="mobile-nav-text">Connexion</span>
                    </a>
                    <a href="{{ path('app_register_patient') }}" class="mobile-nav-link group">
                        <span class="mobile-nav-icon">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M18 9v3m0 0v3m0-3h3m-3 0h-3m-2-5a4 4 0 11-8 0 4 4 0 018 0zM3 20a6 6 0 0112 0v1H3v-1z"/>
                            </svg>
                        </span>
                        <span class="mobile-nav-text">Inscription</span>
                    </a>
                    <button type="button" class="w-full text-left mobile-nav-link group" onclick="document.getElementById('professionalModal').classList.remove('hidden')">
                        <span class="mobile-nav-icon">
                            <svg class="w-5 h-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19.428 15.428a2 2 0 00-1.022-.547l-2.387-.477a6 6 0 00-3.86.517l-.318.158a6 6 0 01-3.86.517L6.05 15.21a2 2 0 00-1.806.547M8 4h8l-1 1v5.172a2 2 0 00.586 1.414l5 5c1.26 1.26.367 3.414-1.415 3.414H4.828c-1.782 0-2.674-2.154-1.414-3.414l5-5A2 2 0 009 10.172V5L8 4z"/>
                            </svg>
                        </span>
                        <span class="mobile-nav-text">Professionnel</span>
                    </button>
                </div>
            </div>
        </header>

        {# Main Content - Centered Auth Form #}
        <main id="main-content" class="flex items-center justify-center flex-grow px-4 py-12 sm:px-6 lg:px-8 bg-gradient-to-br from-wellcare-50 via-white to-wellcare-light-50 dark:from-gray-950 dark:via-gray-900 dark:to-wellcare-dark-900" tabindex="-1">
            <div class="w-full {{ auth_container_class|default('max-w-md') }} page-content">
                {# Auth Card Container #}
                <div class="p-8 bg-white shadow-xl dark:bg-gray-900 rounded-2xl shadow-gray-200/50 dark:shadow-black/20">
                    {% block auth_header %}
                        {% if block('auth_heading') is defined or block('auth_description') is defined %}
                            <div class="mb-8 text-center">
                                {% if block('auth_heading') is defined %}
                                    <h1 class="text-2xl font-bold text-gray-900 dark:text-white">{% block auth_heading %}{% endblock %}</h1>
                                {% endif %}
                                {% if block('auth_description') is defined %}
                                    <p class="mt-2 text-sm text-gray-600 dark:text-gray-400">{% block auth_description %}{% endblock %}</p>
                                {% endif %}
                            </div>
                        {% endif %}
                    {% endblock %}
                    
                    {% block body %}
                    {% endblock %}
                    
                    {% block auth_form %}
                    {% endblock %}
                    
                    {% if block('auth_footer') is defined %}
                        <div class="mt-6 text-center">
                            {% block auth_footer %}{% endblock %}
                        </div>
                    {% endif %}
                    
                    {% if block('auth_below') is defined %}
                        <div class="mt-6 text-xs text-center text-gray-500 dark:text-gray-400">
                            {% block auth_below %}{% endblock %}
                        </div>
                    {% endif %}
                </div>
            </div>
        </main>

        {# Minimal Footer #}
        <footer class="py-6 bg-white border-t border-gray-200 dark:border-gray-800 dark:bg-gray-950">
            <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
                <div class="flex flex-col items-center justify-between gap-4 sm:flex-row">
                    <p class="text-sm text-gray-500 dark:text-gray-400">
                        &copy; {{ "now"|date("Y") }} WellCare Connect.
                    </p>
                    <div class="flex gap-4 text-sm">
                        <a href="{{ path('app_terms') }}" class="text-gray-500 transition-colors hover:text-wellcare-600 dark:text-gray-400 dark:hover:text-wellcare-400">Conditions</a>
                        <a href="#" class="text-gray-500 transition-colors hover:text-wellcare-600 dark:text-gray-400 dark:hover:text-wellcare-400">Confidentialité</a>
                    </div>
                </div>
            </div>
        </footer>
    </div>

    {# Professional Registration Modal - Only visible when triggered #}
    <div id="professionalModal" x-data="{ open: false }" @keydown.escape.window="open = false; document.getElementById('professionalModal').classList.add('hidden')" class="fixed inset-0 z-50 hidden overflow-y-auto" aria-labelledby="modal-title" role="dialog" aria-modal="true">
        <div class="flex items-end justify-center min-h-screen px-4 pt-4 pb-20 text-center sm:block sm:p-0">
            <div class="fixed inset-0 transition-opacity bg-gray-900/75 dark:bg-gray-950/90" aria-hidden="true" onclick="document.getElementById('professionalModal').classList.add('hidden')"></div>
            <span class="hidden sm:inline-block sm:align-middle sm:h-screen" aria-hidden="true">&#8203;</span>
            <div class="inline-block overflow-hidden text-left align-bottom transition-all transform bg-white shadow-2xl dark:bg-gray-900 rounded-2xl sm:align-middle sm:max-w-4xl sm:w-full">
                <div class="px-4 pt-5 pb-4 bg-white dark:bg-gray-900 sm:p-6 sm:pb-4">
                    <div class="absolute top-0 right-0 pt-4 pr-4">
                        <button type="button" class="text-gray-400 hover:text-gray-500 dark:text-gray-500 dark:hover:text-gray-400" onclick="document.getElementById('professionalModal').classList.add('hidden')">
                            <svg class="w-6 h-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
                            </svg>
                        </button>
                    </div>
                    
                    <div class="sm:flex sm:items-start">
                        <div class="w-full mt-3 text-center sm:mt-0 sm:text-left">
                            <h3 class="text-2xl font-bold leading-6 text-gray-900 dark:text-gray-100" id="modal-title">
                                Choisissez votre profession
                            </h3>
                            <p class="mt-2 text-sm text-gray-500 dark:text-gray-400">
                                Sélectionnez le type de compte professionnel que vous souhaitez créer
                            </p>
                            
                            <div class="grid gap-6 mt-6 sm:grid-cols-3">
                                {# Médecin Card #}
                                <a href="{{ path('app_register_medecin') }}" class="relative flex flex-col p-6 transition-all duration-300 border-2 border-gray-200 dark:border-gray-700 rounded-2xl hover:border-wellcare-500 dark:hover:border-wellcare-500 hover:shadow-lg group">
                                    <div class="flex items-center justify-center w-16 h-16 mx-auto mb-4 transition-colors rounded-2xl bg-red-50 dark:bg-red-900/20 group-hover:bg-red-100 dark:group-hover:bg-red-900/30">
                                        <svg class="w-8 h-8 text-red-600 dark:text-red-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4.318 6.318a4.5 4.5 0 000 6.364L12 20.364l7.682-7.682a4.5 4.5 0 00-6.364-6.364L12 7.636l-1.318-1.318a4.5 4.5 0 00-6.364 0z"/>
                                        </svg>
                                    </div>
                                    <h4 class="text-lg font-semibold text-center text-gray-900 dark:text-gray-100">Médecin</h4>
                                    <p class="mt-2 text-sm text-center text-gray-500 dark:text-gray-400">
                                        Consultez vos patients, gérez les rendez-vous et proposez des téléconsultations
                                    </p>
                                    <span class="flex items-center justify-center gap-1 mt-4 text-xs font-medium text-red-600 dark:text-red-400">
                                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                                        </svg>
                                        Vérification requise
                                    </span>
                                </a>
                                
                                {# Coach Card #}
                                <a href="{{ path('app_register_coach') }}" class="relative flex flex-col p-6 transition-all duration-300 border-2 border-gray-200 dark:border-gray-700 rounded-2xl hover:border-wellcare-500 dark:hover:border-wellcare-500 hover:shadow-lg group">
                                    <div class="flex items-center justify-center w-16 h-16 mx-auto mb-4 transition-colors rounded-2xl bg-blue-50 dark:bg-blue-900/20 group-hover:bg-blue-100 dark:group-hover:bg-blue-900/30">
                                        <svg class="w-8 h-8 text-blue-600 dark:text-blue-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                                        </svg>
                                    </div>
                                    <h4 class="text-lg font-semibold text-center text-gray-900 dark:text-gray-100">Coach</h4>
                                    <p class="mt-2 text-sm text-center text-gray-500 dark:text-gray-400">
                                        Accompagnez vos clients dans leur parcours fitness et bien-être
                                    </p>
                                    <span class="flex items-center justify-center gap-1 mt-4 text-xs font-medium text-blue-600 dark:text-blue-400">
                                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                                        </svg>
                                        Vérification requise
                                    </span>
                                </a>
                                
                                {# Nutritionist Card #}
                                <a href="{{ path('app_register_nutritionist') }}" class="relative flex flex-col p-6 transition-all duration-300 border-2 border-gray-200 dark:border-gray-700 rounded-2xl hover:border-wellcare-500 dark:hover:border-wellcare-500 hover:shadow-lg group">
                                    <div class="flex items-center justify-center w-16 h-16 mx-auto mb-4 transition-colors rounded-2xl bg-green-50 dark:bg-green-900/20 group-hover:bg-green-100 dark:group-hover:bg-green-900/30">
                                        <svg class="w-8 h-8 text-green-600 dark:text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"/>
                                        </svg>
                                    </div>
                                    <h4 class="text-lg font-semibold text-center text-gray-900 dark:text-gray-100">Nutritionniste</h4>
                                    <p class="mt-2 text-sm text-center text-gray-500 dark:text-gray-400">
                                        Créez des plans nutritionnels et suivez les régimes alimentaires
                                    </p>
                                    <span class="flex items-center justify-center gap-1 mt-4 text-xs font-medium text-green-600 dark:text-green-400">
                                        <svg class="w-4 h-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"/>
                                        </svg>
                                        Vérification requise
                                    </span>
                                </a>
                            </div>
                            
                            <div class="p-4 mt-6 bg-gray-50 dark:bg-gray-800 rounded-xl">
                                <p class="text-sm text-center text-gray-600 dark:text-gray-300">
                                    <svg class="inline w-4 h-4 mr-1" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                                    </svg>
                                    Tous les comptes professionnels nécessitent une vérification par l'administrateur sous 24-48 heures.
                                </p>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    {% block javascripts %}
        {{ encore_entry_script_tags('app') }}
    {% endblock %}
</body>
</html>
