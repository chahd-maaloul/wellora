<form method="post" action="{{ path('app_nutritionist_delete', {'id': nutritionist.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nutritionist.id) }}">
    <button class="btn">Delete</button>
</form>
