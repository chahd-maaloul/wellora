<form method="post" action="{{ path('app_healthentry_delete', {'id': healthentry.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ healthentry.id) }}">
    <button class="btn">Delete</button>
</form>
