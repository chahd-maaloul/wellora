<form method="post" action="{{ path('app_publication_parcours_delete', {'id': publication_parcour.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ publication_parcour.id) }}">
    <button class="btn">Delete</button>
</form>
