<form method="post" action="{{ path('app_parcours_de_sante_delete', {'id': parcours_de_sante.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ parcours_de_sante.id) }}">
    <button type="submit" class="inline-flex items-center justify-center w-10 h-10 text-white bg-red-600 rounded-lg hover:bg-red-700" title="Delete" aria-label="Delete">
        <i class="fa-solid fa-trash"></i>
    </button>
</form>
