<form method="post" action="{{ path('app_water_log_delete', {'id': water_log.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ water_log.id) }}">
    <button class="btn">Delete</button>
</form>
